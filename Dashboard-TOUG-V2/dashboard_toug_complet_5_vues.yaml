views:
  - type: sections
    max_columns: 4
    icon: mdi:tune-variant
    path: ""
    sections:
      - type: grid
        cards:
          - type: custom:bubble-card
            card_type: separator
            name: Commandes  Chauffage Clim
            icon: mdi:tune-variant
            rows: 1
            grid_options:
              columns: 12
              rows: 0.7
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: select
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:air-conditioner
            name: Mode air
            grid_options:
              columns: full
              rows: 1
            styles: |-
              :host {
                 .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
              }
            scrolling_effect: true
            entity: select.toug_aquaair_mode_air
            sub_button:
              main:
                - entity: sensor.toug_aquaair_temperature_exterieure_exterieur_thoa
                  sub_button_type: select
                  show_icon: false
                  show_state: true
                  name: Ext
                  scrolling_effect: false
                  show_name: true
                  show_arrow: false
                  state_background: false
                  show_background: false
                - entity: sensor.toug_aquaair_temperature_piece_principale
                  sub_button_type: default
                  show_state: true
                  show_icon: true
                  show_name: false
                  show_background: true
                  state_background: true
              bottom: []
            show_name: true
            show_attribute: false
            show_last_updated: false
            attribute: friendly_name
          - type: custom:bubble-card
            card_type: climate
            force_icon: false
            show_state: false
            show_last_changed: false
            icon: mdi:thermostat-box
            name: Salon
            grid_options:
              columns: full
              rows: 1
            styles: |-
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
                }
            scrolling_effect: true
            entity: climate.salon_2
            sub_button:
              main:
                - name: HVAC modes menu
                  select_attribute: hvac_modes
                  state_background: false
                  show_arrow: false
                  sub_button_type: select
                - entity: sensor.toug_aquaair_temperature_salon
                  show_state: true
              bottom: []
            show_icon: true
            show_name: true
          - type: custom:bubble-card
            card_type: climate
            force_icon: false
            show_state: false
            show_last_changed: false
            icon: mdi:thermostat-box
            name: Chambre invités
            grid_options:
              columns: full
              rows: 1
            styles: |-
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
                }
            scrolling_effect: true
            entity: climate.chambre_invites
            show_icon: true
            sub_button:
              main:
                - name: HVAC modes menu
                  select_attribute: hvac_modes
                  state_background: false
                  show_arrow: false
                  sub_button_type: select
                - entity: sensor.toug_aquaair_temperature_chambre_invites
                  show_state: true
                  show_icon: true
                  state_background: true
              bottom: []
          - type: custom:bubble-card
            card_type: climate
            force_icon: false
            show_state: false
            show_last_changed: false
            icon: mdi:thermostat-box
            name: Bureau
            grid_options:
              columns: full
              rows: 1
            styles: |-
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
                }
            scrolling_effect: true
            entity: climate.bureau
            sub_button:
              main:
                - name: HVAC modes menu
                  select_attribute: hvac_modes
                  state_background: false
                  show_arrow: false
                  sub_button_type: select
                - entity: sensor.toug_aquaair_temperature_bureau
                  show_state: true
              bottom: []
          - type: custom:bubble-card
            card_type: climate
            button_type: slider
            styles: |-
              .bubble-range-fill { 
                background: #85a210 !important;
                opacity: 1 !important;
              }
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
              }
            show_icon: true
            show_last_changed: false
            show_attribute: false
            force_icon: false
            show_state: false
            slider_live_update: false
            icon: mdi:thermostat-box
            name: Chambre Georges
            grid_options:
              columns: full
              rows: 1
            entity: climate.chambre_georges
            scrolling_effect: true
            sub_button:
              main:
                - name: HVAC modes menu
                  select_attribute: hvac_modes
                  state_background: false
                  show_arrow: false
                  sub_button_type: select
                - entity: sensor.toug_aquaair_temperature_chambre_georges
                  show_state: true
              bottom: []
          - type: custom:bubble-card
            card_type: separator
            name: Capteurs Chauffage Clim
            icon: mdi:eye-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: button
            button_type: state
            animated_icons: true
            entity: sensor.b_pac_power
            styles: |-
              :host {
                  --color-ok: #bf8d1a;
                  --color-background: var(--bubble-button-main-background-color, var(--bubble-main-background-color, var(--background-color-2, var(--secondary-background-color))));
                  --color-notification-bubble: --color-notification-bubble: #FFFACD;
                  --color-bar: var(--color-ok);
                  /* Calcul de la progress % prod max de 1500 W) */
                  --progress: ${Math.round((Number(state) < 1500 ? Number(state) : 1500) / 1500 * 100)}%;
                }
              .bubble-button-card-container {
                background: var(--color-background) !important;
                position: relative;
              }

              /* Barre de progression qui évolue en fonction de la prod */
              .bubble-button-card-container::before {
                content: "";
                position: absolute;
                left: 0;
                top: 0;
                height: 100%;
                width: var(--progress) !important;
                background: var(--color-bar) !important;
                z-index: 0;
              }
              .bubble-icon {
                color: #bf8d1a !important;
              }
            name: PAC + Appoint ECS Chauf Clim
            icon: mdi:air-conditioner
            sub_button:
              main:
                - entity: sensor.b_pac_power
                  show_state: true
                  show_icon: false
                  state_background: false
                  show_name: true
              bottom: []
            card_layout: large
            grid_options:
              columns: 12
            show_state: false
            scrolling_effect: false
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Pres stat cible
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_pression_statique_cible
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Vit air cible
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_vitesse_air_cible
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Déb air cible
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_debit_air_cible
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Ventilation UI
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_vitesse_ventilateur_ui
            grid_options:
              columns: 12
              rows: 1
          - type: custom:bubble-card
            card_type: separator
            name: Configuration T.One AquaAir
            icon: mdi:cog-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: button
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:calendar
            name: >-
              Date / Heure en UTC (saisir -1h en hiv ou -2h en été à l'heure
              locale => ESPHome actualise en UTC+1 ou UTC+2)
            grid_options:
              columns: 12
              rows: 1
            styles: |
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
                }
            entity: datetime.toug_aquaair_reglage_date_heure
            scrolling_effect: true
            sub_button:
              main: []
              bottom: []
            button_type: state
            rows: 1
            event_action:
              tap_action:
                action: more-info
              double_tap_action:
                action: none
              hold_action:
                action: none
          - type: custom:bubble-card
            card_type: button
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:calendar
            name: Fin vacances
            grid_options:
              columns: 6
              rows: 1
            styles: |-
              .bubble-icon {
                color: #00cc00 !important;
                opacity: 1 !important;
                }
            entity: datetime.toug_aquaair_vacances_fin
            scrolling_effect: true
            sub_button:
              main: []
              bottom: []
            button_type: state
            rows: 1
            event_action:
              tap_action:
                action: more-info
              double_tap_action:
                action: none
              hold_action:
                action: none
          - type: custom:bubble-card
            card_type: button
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:calendar
            name: Début vacances
            grid_options:
              columns: 6
              rows: 1
            styles: |-
              .bubble-icon {
                color: #00cc00 !important;
                opacity: 1 !important;
                }
            entity: datetime.toug_aquaair_vacances_debut
            scrolling_effect: true
            sub_button:
              main: []
              bottom: []
            button_type: state
            rows: 1
            event_action:
              tap_action:
                action: more-info
              double_tap_action:
                action: none
              hold_action:
                action: none
          - type: custom:bubble-card
            card_type: button
            button_type: switch
            styles: |
              :host {
                 .bubble-icon {
                color: #ffcc00 !important;
              }
            name: Vacances
            grid_options:
              columns: 6
              rows: 1
            show_state: false
            slider_live_update: true
            entity: switch.toug_aquaair_vacances
            icon: mdi:beach
            read_only_slider: false
            tap_to_slide: false
            relative_slide: true
            step: 50
            max_value: 2400
            min_value: 0
            scrolling_effect: false
            show_icon: true
            sub_button:
              main: []
              bottom: []
            button_action: {}
          - type: custom:bubble-card
            card_type: button
            button_type: switch
            styles: |-
              :host {
                 .bubble-icon {
                color: #0066cc !important;
              }
            name: Hors gel
            grid_options:
              columns: 6
              rows: 1
            show_state: false
            slider_live_update: true
            entity: switch.toug_aquaair_hors_gel
            icon: mdi:snowflake
            scrolling_effect: false
            show_last_changed: false
            relative_slide: true
            min_value: 0
            max_value: 2400
            step: 50
            sub_button:
              main: []
              bottom: []
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: false
                card_layout: large
                icon: mdi:currency-eur
                name: HP
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                  .bubble-icon {
                    color: #00cc00 !important;
                    opacity: 1 !important;
                    }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: number.toug_aquaair_tarif_hp
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: false
                card_layout: large
                icon: mdi:currency-eur
                name: HC
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                  .bubble-icon {
                    color: #00cc00 !important;
                    opacity: 1 !important;
                    }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: number.toug_aquaair_tarif_hc
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: true
                card_layout: large
                icon: mdi:hazard-lights
                name: Reset kWh
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                  .bubble-icon {
                    color: #ff0000 !important;
                    opacity: 1 !important;
                    }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: button.toug_aquaair_reset_consommation
            grid_options:
              columns: 12
              rows: 1
          - type: custom:bubble-card
            card_type: separator
            name: Diagnostic T.One AquaAir
            icon: mdi:gauge-low
            rows: 1
            grid_options:
              columns: 12
              rows: 0.7
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Défaut PAC
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_defaut_unite_exterieure
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Défauts
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_defauts
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Etat du filtre
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_etat_filtre
            grid_options:
              columns: 12
              rows: 1
          - type: custom:bubble-card
            card_type: select
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:tools
            name: Test
            grid_options:
              columns: 6
              rows: 1
            styles: |-
              :host {
                 .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
              }
            scrolling_effect: true
            entity: select.toug_aquaair_test
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: button
            button_type: state
            styles: |-
              .bubble-range-fill { 
                background: #85a210 !important;
                opacity: 1 !important;
              }
              .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
              }
            show_icon: true
            show_last_changed: false
            show_attribute: false
            force_icon: false
            show_state: false
            slider_live_update: false
            icon: mdi:restart
            name: Redémarrer T.One
            grid_options:
              columns: 6
              rows: 1
            entity: button.toug_aquaair_toug_aquaair_restart
            scrolling_effect: false
            sub_button:
              main: []
              bottom: []
    title: Réglages et contrôles
    cards: []
  - type: sections
    max_columns: 4
    title: ECS
    path: ecs
    icon: mdi:shower-head
    sections:
      - type: grid
        cards:
          - show_current: true
            show_forecast: true
            type: weather-forecast
            entity: weather.eyguieres
            forecast_type: daily
          - type: custom:bubble-card
            card_type: separator
            name: Commandes ECS
            icon: mdi:tune-variant
            rows: 1
            grid_options:
              columns: 12
              rows: 0.7
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: select
            force_icon: false
            show_state: true
            show_last_changed: false
            icon: mdi:air-conditioner
            name: Mode ECS
            grid_options:
              rows: 1
            styles: |-
              :host {
                 .bubble-icon {
                color: #bf8d1a !important;
                opacity: 1 !important;
              }
            scrolling_effect: true
            entity: select.toug_aquaair_mode_ecs
            sub_button:
              main:
                - entity: sensor.toug_aquaair_temperature_exterieure_exterieur_thoa
                  sub_button_type: default
                  show_arrow: false
                  show_state: true
                  icon: mdi:thermometer
                  name: T° ext
                  show_name: true
                  scrolling_effect: false
                  show_background: false
              bottom: []
          - type: custom:bubble-card
            card_type: separator
            name: Capteurs ECS
            icon: mdi:eye-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: button
            button_type: state
            animated_icons: true
            entity: sensor.b_pac_power
            styles: |-
              :host {
                  --color-ok: #bf8d1a;
                  --color-background: var(--bubble-button-main-background-color, var(--bubble-main-background-color, var(--background-color-2, var(--secondary-background-color))));
                  --color-notification-bubble: --color-notification-bubble: #FFFACD;
                  --color-bar: var(--color-ok);
                  /* Calcul de la progress % prod max de 1500 W) */
                  --progress: ${Math.round((Number(state) < 1500 ? Number(state) : 1500) / 1500 * 100)}%;
                }
              .bubble-button-card-container {
                background: var(--color-background) !important;
                position: relative;
              }

              /* Barre de progression qui évolue en fonction de la prod */
              .bubble-button-card-container::before {
                content: "";
                position: absolute;
                left: 0;
                top: 0;
                height: 100%;
                width: var(--progress) !important;
                background: var(--color-bar) !important;
                z-index: 0;
              }
              .bubble-icon {
                color: #bf8d1a !important;
              }
            name: PAC + Appoint ECS Chauf Clim
            icon: mdi:air-conditioner
            sub_button:
              main:
                - entity: sensor.b_pac_power
                  show_state: true
                  show_icon: false
                  state_background: false
                  show_name: true
              bottom: []
            card_layout: large
            grid_options:
              columns: 12
            show_state: false
            scrolling_effect: false
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Consigne ECS nuit
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_consigne_ecs_telecommande
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: ECS tout élec
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: binary_sensor.toug_aquaair_ecs_tout_electrique
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Echangeur EC
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_echangeur_eau_chaude
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: ECS %
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_pourcentage_ecs
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: T° ECS
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_temperature_ecs
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Sonde ECS basse
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_sonde_ecs_bas
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: true
                card_layout: large
                icon: ""
                name: Sonde ECS haute
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_sonde_ecs_haut
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Vol ECS
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_volume_restant_ecs
            grid_options:
              columns: 12
              rows: 1
          - type: custom:bubble-card
            card_type: separator
            name: Configuration ECS
            icon: mdi:cog-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: false
                card_layout: large
                icon: mdi:account-group
                name: Foyer
                button_type: state
                styles: ".bubble-button-card-container {\n  border-radius: 32px 0 0 32px !important;\n}\n.bubble-icon {\n  color: \t#DEB887 !important;\n  opacity: 1 !important;\n  }"
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: number.toug_aquaair_composition_foyer
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: true
                card_layout: large
                icon: mdi:coolant-temperature
                name: Consigne ECS relance
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                  .bubble-icon {
                    color: #0066ff !important;
                    opacity: 1 !important;
                    }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: number.toug_aquaair_consigne_ecs
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: true
                scrolling_effect: true
                card_layout: large
                icon: mdi:history
                name: Reset anode
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                  .bubble-icon {
                    color: #ff0000 !important;
                    opacity: 1 !important;
                    }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: button.toug_aquaair_reset_anode
            grid_options:
              columns: 12
              rows: 1
    cards: []
  - type: sections
    max_columns: 4
    title: Thermostats
    path: thermostats
    icon: mdi:home-thermometer-outline
    sections:
      - type: grid
        cards:
          - type: heading
            heading: Températures extérieures
            heading_style: title
            icon: mdi:thermometer
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: T° ext. PAC
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_temperature_exterieure_exterieur_thoa
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: T° ext portillon
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.detecteur_portillon_temperature
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: T° ext garage
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.detecteur_garage_temperature
            grid_options:
              columns: 12
              rows: 1
          - type: heading
            heading: Thermostats
            heading_style: title
            icon: mdi:thermostat-cog
          - type: thermostat
            entity: climate.salon_2
          - type: thermostat
            entity: climate.chambre_invites
          - type: thermostat
            entity: climate.bureau
          - type: thermostat
            entity: climate.chambre_georges
        column_span: 2
    subview: false
    cards: []
    show_icon_and_title: false
  - type: sections
    max_columns: 4
    title: Programmation
    path: Prog
    icon: mdi:calendar-clock
    sections:
      - type: grid
        cards:
          - type: heading
            heading: Programmation horaire
            heading_style: title
            icon: mdi:home-clock
          - type: tile
            entity: input_select.programmation_horaire
            name: Sélection du programme
            show_entity_picture: false
            hide_state: true
            vertical: false
            features:
              - type: select-options
            features_position: bottom
            grid_options:
              columns: 9
              rows: 2
          - type: vertical-stack
            cards:
              - type: tile
                entity: sensor.toug_aquaair_version_logiciel_regulateur
                name: Confort
                icon: mdi:rectangle
                color: orange
                hide_state: true
                vertical: false
                tap_action:
                  action: none
                features_position: bottom
              - type: tile
                entity: sensor.toug_aquaair_version_logiciel_regulateur
                name: Eco
                icon: mdi:rectangle-outline
                color: disabled
                show_entity_picture: false
                hide_state: true
                vertical: false
                tap_action:
                  action: none
                features_position: bottom
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: or
                conditions:
                  - condition: state
                    entity: input_select.programmation_horaire
                    state: Chauffage Progr. A
                  - condition: state
                    entity: input_select.programmation_horaire
                    state: Chauffage Progr. B
          - type: vertical-stack
            cards:
              - type: tile
                entity: sensor.toug_aquaair_version_logiciel_regulateur
                name: Confort
                icon: mdi:rectangle
                color: light-blue
                hide_state: true
                vertical: false
                tap_action:
                  action: none
                features_position: bottom
              - type: tile
                entity: sensor.toug_aquaair_version_logiciel_regulateur
                name: Eco
                icon: mdi:rectangle-outline
                color: disabled
                show_entity_picture: false
                hide_state: true
                vertical: false
                tap_action:
                  action: none
                features_position: bottom
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: or
                conditions:
                  - condition: state
                    entity: input_select.programmation_horaire
                    state: Rafraîchissement Progr. C
                  - condition: state
                    entity: input_select.programmation_horaire
                    state: Rafraîchissement Progr. D
        column_span: 3
      - type: grid
        cards:
          - type: custom:button-card
            grid_options:
              columns: full
            styles:
              card:
                - overflow: auto
              grid:
                - grid-template-columns: max-content repeat(24, 1fr)
                - grid-template-rows: auto repeat(7, 1fr)
                - grid-template-areas: >
                    "header_day h0_title h1_title h2_title h3_title h4_title
                    h5_title h6_title h7_title h8_title h9_title h10_title
                    h11_title h12_title h13_title h14_title h15_title h16_title
                    h17_title h18_title h19_title h20_title h21_title h22_title
                    h23_title"

                    "label_lun h0_lun h1_lun h2_lun h3_lun h4_lun h5_lun h6_lun
                    h7_lun h8_lun h9_lun h10_lun h11_lun h12_lun h13_lun h14_lun
                    h15_lun h16_lun h17_lun h18_lun h19_lun h20_lun h21_lun
                    h22_lun h23_lun"

                    "label_mar h0_mar h1_mar h2_mar h3_mar h4_mar h5_mar h6_mar
                    h7_mar h8_mar h9_mar h10_mar h11_mar h12_mar h13_mar h14_mar
                    h15_mar h16_mar h17_mar h18_mar h19_mar h20_mar h21_mar
                    h22_mar h23_mar"

                    "label_mer h0_mer h1_mer h2_mer h3_mer h4_mer h5_mer h6_mer
                    h7_mer h8_mer h9_mer h10_mer h11_mer h12_mer h13_mer h14_mer
                    h15_mer h16_mer h17_mer h18_mer h19_mer h20_mer h21_mer
                    h22_mer h23_mer"

                    "label_jeu h0_jeu h1_jeu h2_jeu h3_jeu h4_jeu h5_jeu h6_jeu
                    h7_jeu h8_jeu h9_jeu h10_jeu h11_jeu h12_jeu h13_jeu h14_jeu
                    h15_jeu h16_jeu h17_jeu h18_jeu h19_jeu h20_jeu h21_jeu
                    h22_jeu h23_jeu"

                    "label_ven h0_ven h1_ven h2_ven h3_ven h4_ven h5_ven h6_ven
                    h7_ven h8_ven h9_ven h10_ven h11_ven h12_ven h13_ven h14_ven
                    h15_ven h16_ven h17_ven h18_ven h19_ven h20_ven h21_ven
                    h22_ven h23_ven"

                    "label_sam h0_sam h1_sam h2_sam h3_sam h4_sam h5_sam h6_sam
                    h7_sam h8_sam h9_sam h10_sam h11_sam h12_sam h13_sam h14_sam
                    h15_sam h16_sam h17_sam h18_sam h19_sam h20_sam h21_sam
                    h22_sam h23_sam"

                    "label_dim h0_dim h1_dim h2_dim h3_dim h4_dim h5_dim h6_dim
                    h7_dim h8_dim h9_dim h10_dim h11_dim h12_dim h13_dim h14_dim
                    h15_dim h16_dim h17_dim h18_dim h19_dim h20_dim h21_dim
                    h22_dim h23_dim"
            hour_title:
              type: custom:button-card
              styles:
                name:
                  - font-size: 0.8em
                  - margin-left: "-100%"
                card:
                  - border-style: none
                  - overflow: visible
            day_label:
              type: custom:button-card
              styles:
                name:
                  - font-weight: bold
                card:
                  - border-style: none
            empty_cell:
              card:
                type: custom:button-card
                label: ​
                show_label: true
                styles:
                  card:
                    - border-radius: 5px
                    - min-width: 15px
            custom_fields:
              header_day:
                card:
                  type: custom:button-card
                  color_type: blank-card
              h0_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "0"
              h1_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "1"
              h2_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "2"
              h3_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "3"
              h4_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "4"
              h5_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "5"
              h6_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "6"
              h7_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "7"
              h8_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "8"
              h9_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "9"
              h10_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "10"
              h11_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "11"
              h12_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "12"
              h13_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "13"
              h14_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "14"
              h15_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "15"
              h16_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "16"
              h17_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "17"
              h18_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "18"
              h19_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "19"
              h20_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "20"
              h21_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "21"
              h22_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "22"
              h23_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "23"
              label_lun:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Lun
              h0_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 0
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 0; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 1
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 1; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 2
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 2; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 3
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 3; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 4
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 4; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 5
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 5; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 6
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 6; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 7
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 7; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 8
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 8; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 9
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 9; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 10
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 10; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 11
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 11; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 12
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 12; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 13
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 13; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 14
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 14; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 15
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 15; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 16
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 16; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 17
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 17; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 18
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 18; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 19
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 19; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 20
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 20; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 21
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 21; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 22
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 22; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 23
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 23; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_mar:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mar
              h0_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 24
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 24; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 25
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 25; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 26
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 26; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 27
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 27; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 28
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 28; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 29
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 29; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 30
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 30; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 31
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 31; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 32
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 32; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 33
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 33; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 34
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 34; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 35
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 35; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 36
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 36; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 37
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 37; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 38
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 38; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 39
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 39; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 40
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 40; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 41
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 41; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 42
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 42; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 43
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 43; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 44
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 44; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 45
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 45; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 46
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 46; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 47
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 47; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_mer:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mer
              h0_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 48
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 48; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 49
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 49; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 50
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 50; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 51
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 51; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 52
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 52; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 53
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 53; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 54
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 54; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 55
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 55; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 56
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 56; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 57
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 57; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 58
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 58; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 59
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 59; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 60
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 60; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 61
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 61; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 62
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 62; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 63
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 63; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 64
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 64; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 65
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 65; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 66
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 66; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 67
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 67; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 68
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 68; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 69
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 69; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 70
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 70; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 71
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 71; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_jeu:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Jeu
              h0_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 72
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 72; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 73
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 73; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 74
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 74; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 75
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 75; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 76
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 76; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 77
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 77; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 78
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 78; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 79
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 79; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 80
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 80; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 81
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 81; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 82
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 82; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 83
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 83; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 84
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 84; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 85
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 85; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 86
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 86; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 87
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 87; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 88
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 88; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 89
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 89; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 90
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 90; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 91
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 91; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 92
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 92; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 93
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 93; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 94
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 94; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 95
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 95; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_ven:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Ven
              h0_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 96
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 96; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 97
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 97; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 98
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 98; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 99
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 99; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 100
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 100; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 101
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 101; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 102
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 102; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 103
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 103; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 104
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 104; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 105
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 105; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 106
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 106; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 107
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 107; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 108
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 108; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 109
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 109; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 110
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 110; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 111
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 111; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 112
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 112; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 113
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 113; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 114
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 114; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 115
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 115; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 116
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 116; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 117
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 117; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 118
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 118; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 119
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 119; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_sam:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Sam
              h0_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 120
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 120; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 121
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 121; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 122
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 122; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 123
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 123; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 124
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 124; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 125
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 125; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 126
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 126; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 127
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 127; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 128
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 128; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 129
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 129; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 130
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 130; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 131
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 131; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 132
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 132; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 133
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 133; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 134
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 134; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 135
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 135; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 136
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 136; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 137
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 137; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 138
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 138; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 139
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 139; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 140
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 140; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 141
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 141; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 142
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 142; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 143
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 143; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_dim:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Dim
              h0_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 144
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 144; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 145
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 145; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 146
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 146; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 147
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 147; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 148
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 148; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 149
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 149; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 150
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 150; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 151
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 151; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 152
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 152; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 153
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 153; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 154
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 154; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 155
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 155; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 156
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 156; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 157
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 157; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 158
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 158; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 159
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 159; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 160
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 160; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 161
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 161; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 162
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 162; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 163
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 163; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 164
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 164; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 165
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 165; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 166
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 166; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: A
                      index: 167
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.proga_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 167; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
          - show_name: true
            show_icon: true
            type: button
            name: Envoyer
            icon: mdi:send-clock
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: ha_vers_esphome
                prog: A
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state: buffer
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-sync
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: A
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state: dataset
            name: Synchroniser
            show_state: false
          - show_name: true
            show_icon: true
            type: button
            name: Tout remplir
            icon: mdi:calendar-month
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.proga_buffer
              data:
                value: 00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            name: Tout effacer
            icon: mdi:calendar-blank
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.proga_buffer
              data:
                value: "00000000000000000000000000000000000000000000000000000000"
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-remove
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: A
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state: buffer
            name: Annuler
          - show_name: true
            show_icon: true
            type: button
            name: Mise à jour...
            icon: mdi:timer-sand
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: state
                entity: sensor.proga_synchro
                state: maj
            icon_height: 50px
        column_span: 3
        visibility:
          - condition: state
            entity: input_select.programmation_horaire
            state: Chauffage Progr. A
      - type: grid
        cards:
          - type: custom:button-card
            grid_options:
              columns: full
            styles:
              card:
                - overflow: auto
              grid:
                - grid-template-columns: max-content repeat(24, 1fr)
                - grid-template-rows: auto repeat(7, 1fr)
                - grid-template-areas: >
                    "header_day h0_title h1_title h2_title h3_title h4_title
                    h5_title h6_title h7_title h8_title h9_title h10_title
                    h11_title h12_title h13_title h14_title h15_title h16_title
                    h17_title h18_title h19_title h20_title h21_title h22_title
                    h23_title"

                    "label_lun h0_lun h1_lun h2_lun h3_lun h4_lun h5_lun h6_lun
                    h7_lun h8_lun h9_lun h10_lun h11_lun h12_lun h13_lun h14_lun
                    h15_lun h16_lun h17_lun h18_lun h19_lun h20_lun h21_lun
                    h22_lun h23_lun"

                    "label_mar h0_mar h1_mar h2_mar h3_mar h4_mar h5_mar h6_mar
                    h7_mar h8_mar h9_mar h10_mar h11_mar h12_mar h13_mar h14_mar
                    h15_mar h16_mar h17_mar h18_mar h19_mar h20_mar h21_mar
                    h22_mar h23_mar"

                    "label_mer h0_mer h1_mer h2_mer h3_mer h4_mer h5_mer h6_mer
                    h7_mer h8_mer h9_mer h10_mer h11_mer h12_mer h13_mer h14_mer
                    h15_mer h16_mer h17_mer h18_mer h19_mer h20_mer h21_mer
                    h22_mer h23_mer"

                    "label_jeu h0_jeu h1_jeu h2_jeu h3_jeu h4_jeu h5_jeu h6_jeu
                    h7_jeu h8_jeu h9_jeu h10_jeu h11_jeu h12_jeu h13_jeu h14_jeu
                    h15_jeu h16_jeu h17_jeu h18_jeu h19_jeu h20_jeu h21_jeu
                    h22_jeu h23_jeu"

                    "label_ven h0_ven h1_ven h2_ven h3_ven h4_ven h5_ven h6_ven
                    h7_ven h8_ven h9_ven h10_ven h11_ven h12_ven h13_ven h14_ven
                    h15_ven h16_ven h17_ven h18_ven h19_ven h20_ven h21_ven
                    h22_ven h23_ven"

                    "label_sam h0_sam h1_sam h2_sam h3_sam h4_sam h5_sam h6_sam
                    h7_sam h8_sam h9_sam h10_sam h11_sam h12_sam h13_sam h14_sam
                    h15_sam h16_sam h17_sam h18_sam h19_sam h20_sam h21_sam
                    h22_sam h23_sam"

                    "label_dim h0_dim h1_dim h2_dim h3_dim h4_dim h5_dim h6_dim
                    h7_dim h8_dim h9_dim h10_dim h11_dim h12_dim h13_dim h14_dim
                    h15_dim h16_dim h17_dim h18_dim h19_dim h20_dim h21_dim
                    h22_dim h23_dim"
            hour_title:
              type: custom:button-card
              styles:
                name:
                  - font-size: 0.8em
                  - margin-left: "-100%"
                card:
                  - border-style: none
                  - overflow: visible
            day_label:
              type: custom:button-card
              styles:
                name:
                  - font-weight: bold
                card:
                  - border-style: none
            empty_cell:
              card:
                type: custom:button-card
                label: ​
                show_label: true
                styles:
                  card:
                    - border-radius: 5px
                    - min-width: 15px
            custom_fields:
              header_day:
                card:
                  type: custom:button-card
                  color_type: blank-card
              h0_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "0"
              h1_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "1"
              h2_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "2"
              h3_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "3"
              h4_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "4"
              h5_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "5"
              h6_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "6"
              h7_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "7"
              h8_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "8"
              h9_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "9"
              h10_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "10"
              h11_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "11"
              h12_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "12"
              h13_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "13"
              h14_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "14"
              h15_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "15"
              h16_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "16"
              h17_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "17"
              h18_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "18"
              h19_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "19"
              h20_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "20"
              h21_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "21"
              h22_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "22"
              h23_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "23"
              label_lun:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Lun
              h0_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 0
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 0; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 1
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 1; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 2
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 2; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 3
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 3; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 4
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 4; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 5
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 5; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 6
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 6; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 7
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 7; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 8
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 8; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 9
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 9; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 10
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 10; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 11
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 11; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 12
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 12; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 13
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 13; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 14
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 14; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 15
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 15; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 16
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 16; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 17
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 17; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 18
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 18; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 19
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 19; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 20
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 20; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 21
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 21; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 22
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 22; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 23
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 23; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_mar:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mar
              h0_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 24
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 24; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 25
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 25; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 26
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 26; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 27
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 27; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 28
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 28; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 29
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 29; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 30
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 30; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 31
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 31; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 32
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 32; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 33
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 33; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 34
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 34; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 35
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 35; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 36
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 36; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 37
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 37; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 38
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 38; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 39
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 39; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 40
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 40; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 41
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 41; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 42
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 42; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 43
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 43; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 44
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 44; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 45
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 45; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 46
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 46; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 47
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 47; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_mer:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mer
              h0_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 48
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 48; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 49
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 49; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 50
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 50; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 51
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 51; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 52
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 52; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 53
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 53; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 54
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 54; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 55
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 55; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 56
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 56; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 57
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 57; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 58
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 58; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 59
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 59; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 60
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 60; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 61
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 61; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 62
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 62; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 63
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 63; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 64
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 64; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 65
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 65; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 66
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 66; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 67
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 67; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 68
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 68; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 69
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 69; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 70
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 70; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 71
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 71; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_jeu:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Jeu
              h0_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 72
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 72; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 73
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 73; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 74
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 74; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 75
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 75; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 76
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 76; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 77
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 77; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 78
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 78; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 79
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 79; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 80
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 80; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 81
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 81; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 82
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 82; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 83
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 83; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 84
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 84; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 85
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 85; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 86
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 86; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 87
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 87; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 88
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 88; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 89
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 89; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 90
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 90; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 91
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 91; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 92
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 92; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 93
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 93; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 94
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 94; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 95
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 95; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_ven:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Ven
              h0_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 96
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 96; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 97
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 97; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 98
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 98; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 99
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 99; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 100
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 100; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 101
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 101; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 102
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 102; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 103
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 103; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 104
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 104; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 105
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 105; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 106
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 106; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 107
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 107; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 108
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 108; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 109
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 109; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 110
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 110; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 111
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 111; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 112
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 112; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 113
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 113; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 114
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 114; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 115
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 115; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 116
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 116; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 117
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 117; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 118
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 118; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 119
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 119; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_sam:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Sam
              h0_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 120
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 120; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 121
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 121; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 122
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 122; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 123
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 123; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 124
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 124; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 125
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 125; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 126
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 126; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 127
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 127; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 128
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 128; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 129
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 129; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 130
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 130; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 131
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 131; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 132
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 132; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 133
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 133; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 134
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 134; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 135
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 135; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 136
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 136; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 137
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 137; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 138
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 138; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 139
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 139; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 140
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 140; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 141
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 141; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 142
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 142; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 143
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 143; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              label_dim:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Dim
              h0_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 144
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 144; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h1_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 145
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 145; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h2_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 146
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 146; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h3_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 147
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 147; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h4_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 148
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 148; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h5_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 149
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 149; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h6_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 150
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 150; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h7_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 151
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 151; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h8_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 152
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 152; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h9_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 153
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 153; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h10_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 154
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 154; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h11_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 155
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 155; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h12_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 156
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 156; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h13_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 157
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 157; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h14_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 158
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 158; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h15_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 159
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 159; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h16_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 160
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 160; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h17_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 161
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 161; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h18_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 162
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 162; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h19_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 163
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 163; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h20_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 164
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 164; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h21_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 165
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 165; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h22_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 166
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 166; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
              h23_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: B
                      index: 167
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progb_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 167; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "orange" : "white";
                          ]]]
          - show_name: true
            show_icon: true
            type: button
            name: Envoyer
            icon: mdi:send-clock
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: ha_vers_esphome
                prog: B
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state: buffer
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-sync
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: B
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state: dataset
            name: Synchroniser
            show_state: false
          - show_name: true
            show_icon: true
            type: button
            name: Tout remplir
            icon: mdi:calendar-month
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progb_buffer
              data:
                value: 00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            name: Tout effacer
            icon: mdi:calendar-blank
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progb_buffer
              data:
                value: "00000000000000000000000000000000000000000000000000000000"
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-remove
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: B
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state: buffer
            name: Annuler
          - show_name: true
            show_icon: true
            type: button
            name: Mise à jour...
            icon: mdi:timer-sand
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progb_synchro
                state: maj
            icon_height: 50px
        column_span: 3
        visibility:
          - condition: state
            entity: input_select.programmation_horaire
            state: Chauffage Progr. B
      - type: grid
        cards:
          - type: custom:button-card
            grid_options:
              columns: full
            styles:
              card:
                - overflow: auto
              grid:
                - grid-template-columns: max-content repeat(24, 1fr)
                - grid-template-rows: auto repeat(7, 1fr)
                - grid-template-areas: >
                    "header_day h0_title h1_title h2_title h3_title h4_title
                    h5_title h6_title h7_title h8_title h9_title h10_title
                    h11_title h12_title h13_title h14_title h15_title h16_title
                    h17_title h18_title h19_title h20_title h21_title h22_title
                    h23_title"

                    "label_lun h0_lun h1_lun h2_lun h3_lun h4_lun h5_lun h6_lun
                    h7_lun h8_lun h9_lun h10_lun h11_lun h12_lun h13_lun h14_lun
                    h15_lun h16_lun h17_lun h18_lun h19_lun h20_lun h21_lun
                    h22_lun h23_lun"

                    "label_mar h0_mar h1_mar h2_mar h3_mar h4_mar h5_mar h6_mar
                    h7_mar h8_mar h9_mar h10_mar h11_mar h12_mar h13_mar h14_mar
                    h15_mar h16_mar h17_mar h18_mar h19_mar h20_mar h21_mar
                    h22_mar h23_mar"

                    "label_mer h0_mer h1_mer h2_mer h3_mer h4_mer h5_mer h6_mer
                    h7_mer h8_mer h9_mer h10_mer h11_mer h12_mer h13_mer h14_mer
                    h15_mer h16_mer h17_mer h18_mer h19_mer h20_mer h21_mer
                    h22_mer h23_mer"

                    "label_jeu h0_jeu h1_jeu h2_jeu h3_jeu h4_jeu h5_jeu h6_jeu
                    h7_jeu h8_jeu h9_jeu h10_jeu h11_jeu h12_jeu h13_jeu h14_jeu
                    h15_jeu h16_jeu h17_jeu h18_jeu h19_jeu h20_jeu h21_jeu
                    h22_jeu h23_jeu"

                    "label_ven h0_ven h1_ven h2_ven h3_ven h4_ven h5_ven h6_ven
                    h7_ven h8_ven h9_ven h10_ven h11_ven h12_ven h13_ven h14_ven
                    h15_ven h16_ven h17_ven h18_ven h19_ven h20_ven h21_ven
                    h22_ven h23_ven"

                    "label_sam h0_sam h1_sam h2_sam h3_sam h4_sam h5_sam h6_sam
                    h7_sam h8_sam h9_sam h10_sam h11_sam h12_sam h13_sam h14_sam
                    h15_sam h16_sam h17_sam h18_sam h19_sam h20_sam h21_sam
                    h22_sam h23_sam"

                    "label_dim h0_dim h1_dim h2_dim h3_dim h4_dim h5_dim h6_dim
                    h7_dim h8_dim h9_dim h10_dim h11_dim h12_dim h13_dim h14_dim
                    h15_dim h16_dim h17_dim h18_dim h19_dim h20_dim h21_dim
                    h22_dim h23_dim"
            hour_title:
              type: custom:button-card
              styles:
                name:
                  - font-size: 0.8em
                  - margin-left: "-100%"
                card:
                  - border-style: none
                  - overflow: visible
            day_label:
              type: custom:button-card
              styles:
                name:
                  - font-weight: bold
                card:
                  - border-style: none
            empty_cell:
              card:
                type: custom:button-card
                label: ​
                show_label: true
                styles:
                  card:
                    - border-radius: 5px
                    - min-width: 15px
            custom_fields:
              header_day:
                card:
                  type: custom:button-card
                  color_type: blank-card
              h0_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "0"
              h1_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "1"
              h2_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "2"
              h3_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "3"
              h4_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "4"
              h5_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "5"
              h6_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "6"
              h7_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "7"
              h8_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "8"
              h9_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "9"
              h10_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "10"
              h11_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "11"
              h12_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "12"
              h13_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "13"
              h14_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "14"
              h15_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "15"
              h16_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "16"
              h17_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "17"
              h18_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "18"
              h19_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "19"
              h20_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "20"
              h21_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "21"
              h22_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "22"
              h23_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "23"
              label_lun:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Lun
              h0_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 0
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 0; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 1
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 1; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 2
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 2; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 3
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 3; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 4
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 4; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 5
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 5; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 6
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 6; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 7
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 7; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 8
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 8; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 9
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 9; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 10
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 10; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 11
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 11; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 12
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 12; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 13
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 13; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 14
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 14; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 15
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 15; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 16
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 16; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 17
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 17; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 18
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 18; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 19
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 19; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 20
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 20; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 21
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 21; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 22
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 22; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 23
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 23; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_mar:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mar
              h0_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 24
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 24; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 25
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 25; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 26
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 26; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 27
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 27; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 28
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 28; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 29
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 29; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 30
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 30; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 31
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 31; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 32
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 32; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 33
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 33; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 34
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 34; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 35
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 35; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 36
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 36; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 37
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 37; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 38
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 38; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 39
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 39; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 40
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 40; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 41
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 41; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 42
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 42; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 43
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 43; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 44
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 44; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 45
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 45; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 46
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 46; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 47
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 47; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_mer:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mer
              h0_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 48
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 48; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 49
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 49; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 50
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 50; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 51
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 51; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 52
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 52; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 53
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 53; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 54
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 54; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 55
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 55; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 56
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 56; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 57
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 57; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 58
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 58; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 59
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 59; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 60
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 60; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 61
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 61; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 62
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 62; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 63
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 63; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 64
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 64; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 65
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 65; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 66
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 66; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 67
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 67; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 68
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 68; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 69
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 69; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 70
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 70; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 71
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 71; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_jeu:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Jeu
              h0_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 72
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 72; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 73
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 73; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 74
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 74; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 75
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 75; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 76
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 76; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 77
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 77; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 78
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 78; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 79
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 79; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 80
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 80; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 81
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 81; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 82
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 82; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 83
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 83; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 84
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 84; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 85
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 85; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 86
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 86; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 87
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 87; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 88
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 88; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 89
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 89; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 90
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 90; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 91
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 91; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 92
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 92; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 93
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 93; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 94
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 94; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 95
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 95; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_ven:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Ven
              h0_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 96
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 96; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 97
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 97; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 98
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 98; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 99
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 99; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 100
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 100; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 101
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 101; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 102
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 102; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 103
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 103; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 104
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 104; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 105
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 105; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 106
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 106; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 107
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 107; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 108
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 108; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 109
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 109; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 110
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 110; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 111
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 111; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 112
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 112; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 113
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 113; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 114
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 114; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 115
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 115; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 116
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 116; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 117
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 117; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 118
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 118; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 119
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 119; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_sam:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Sam
              h0_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 120
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 120; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 121
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 121; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 122
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 122; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 123
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 123; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 124
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 124; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 125
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 125; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 126
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 126; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 127
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 127; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 128
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 128; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 129
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 129; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 130
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 130; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 131
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 131; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 132
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 132; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 133
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 133; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 134
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 134; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 135
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 135; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 136
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 136; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 137
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 137; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 138
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 138; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 139
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 139; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 140
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 140; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 141
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 141; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 142
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 142; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 143
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 143; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_dim:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Dim
              h0_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 144
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 144; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 145
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 145; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 146
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 146; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 147
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 147; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 148
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 148; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 149
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 149; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 150
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 150; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 151
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 151; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 152
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 152; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 153
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 153; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 154
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 154; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 155
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 155; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 156
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 156; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 157
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 157; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 158
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 158; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 159
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 159; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 160
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 160; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 161
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 161; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 162
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 162; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 163
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 163; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 164
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 164; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 165
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 165; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 166
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 166; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: C
                      index: 167
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progc_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 167; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
          - show_name: true
            show_icon: true
            type: button
            name: Envoyer
            icon: mdi:send-clock
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: ha_vers_esphome
                prog: C
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state: buffer
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-sync
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: C
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state: dataset
            name: Synchroniser
            show_state: false
          - show_name: true
            show_icon: true
            type: button
            name: Tout remplir
            icon: mdi:calendar-month
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progc_buffer
              data:
                value: 00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            name: Tout effacer
            icon: mdi:calendar-blank
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progc_buffer
              data:
                value: "00000000000000000000000000000000000000000000000000000000"
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-remove
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: C
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state: buffer
            name: Annuler
          - show_name: true
            show_icon: true
            type: button
            name: Mise à jour...
            icon: mdi:timer-sand
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progc_synchro
                state: maj
            icon_height: 50px
        column_span: 3
        visibility:
          - condition: state
            entity: input_select.programmation_horaire
            state: Rafraîchissement Progr. C
      - type: grid
        cards:
          - type: custom:button-card
            grid_options:
              columns: full
            styles:
              card:
                - overflow: auto
              grid:
                - grid-template-columns: max-content repeat(24, 1fr)
                - grid-template-rows: auto repeat(7, 1fr)
                - grid-template-areas: >
                    "header_day h0_title h1_title h2_title h3_title h4_title
                    h5_title h6_title h7_title h8_title h9_title h10_title
                    h11_title h12_title h13_title h14_title h15_title h16_title
                    h17_title h18_title h19_title h20_title h21_title h22_title
                    h23_title"

                    "label_lun h0_lun h1_lun h2_lun h3_lun h4_lun h5_lun h6_lun
                    h7_lun h8_lun h9_lun h10_lun h11_lun h12_lun h13_lun h14_lun
                    h15_lun h16_lun h17_lun h18_lun h19_lun h20_lun h21_lun
                    h22_lun h23_lun"

                    "label_mar h0_mar h1_mar h2_mar h3_mar h4_mar h5_mar h6_mar
                    h7_mar h8_mar h9_mar h10_mar h11_mar h12_mar h13_mar h14_mar
                    h15_mar h16_mar h17_mar h18_mar h19_mar h20_mar h21_mar
                    h22_mar h23_mar"

                    "label_mer h0_mer h1_mer h2_mer h3_mer h4_mer h5_mer h6_mer
                    h7_mer h8_mer h9_mer h10_mer h11_mer h12_mer h13_mer h14_mer
                    h15_mer h16_mer h17_mer h18_mer h19_mer h20_mer h21_mer
                    h22_mer h23_mer"

                    "label_jeu h0_jeu h1_jeu h2_jeu h3_jeu h4_jeu h5_jeu h6_jeu
                    h7_jeu h8_jeu h9_jeu h10_jeu h11_jeu h12_jeu h13_jeu h14_jeu
                    h15_jeu h16_jeu h17_jeu h18_jeu h19_jeu h20_jeu h21_jeu
                    h22_jeu h23_jeu"

                    "label_ven h0_ven h1_ven h2_ven h3_ven h4_ven h5_ven h6_ven
                    h7_ven h8_ven h9_ven h10_ven h11_ven h12_ven h13_ven h14_ven
                    h15_ven h16_ven h17_ven h18_ven h19_ven h20_ven h21_ven
                    h22_ven h23_ven"

                    "label_sam h0_sam h1_sam h2_sam h3_sam h4_sam h5_sam h6_sam
                    h7_sam h8_sam h9_sam h10_sam h11_sam h12_sam h13_sam h14_sam
                    h15_sam h16_sam h17_sam h18_sam h19_sam h20_sam h21_sam
                    h22_sam h23_sam"

                    "label_dim h0_dim h1_dim h2_dim h3_dim h4_dim h5_dim h6_dim
                    h7_dim h8_dim h9_dim h10_dim h11_dim h12_dim h13_dim h14_dim
                    h15_dim h16_dim h17_dim h18_dim h19_dim h20_dim h21_dim
                    h22_dim h23_dim"
            hour_title:
              type: custom:button-card
              styles:
                name:
                  - font-size: 0.8em
                  - margin-left: "-100%"
                card:
                  - border-style: none
                  - overflow: visible
            day_label:
              type: custom:button-card
              styles:
                name:
                  - font-weight: bold
                card:
                  - border-style: none
            empty_cell:
              card:
                type: custom:button-card
                label: ​
                show_label: true
                styles:
                  card:
                    - border-radius: 5px
                    - min-width: 15px
            custom_fields:
              header_day:
                card:
                  type: custom:button-card
                  color_type: blank-card
              h0_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "0"
              h1_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "1"
              h2_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "2"
              h3_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "3"
              h4_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "4"
              h5_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "5"
              h6_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "6"
              h7_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "7"
              h8_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "8"
              h9_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "9"
              h10_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "10"
              h11_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "11"
              h12_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "12"
              h13_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "13"
              h14_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "14"
              h15_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "15"
              h16_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "16"
              h17_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "17"
              h18_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "18"
              h19_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "19"
              h20_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "20"
              h21_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "21"
              h22_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "22"
              h23_title:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-size: 0.8em
                      - margin-left: "-100%"
                    card:
                      - border-style: none
                      - overflow: visible
                  name: "23"
              label_lun:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Lun
              h0_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 0
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 0; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 1
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 1; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 2
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 2; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 3
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 3; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 4
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 4; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 5
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 5; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 6
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 6; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 7
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 7; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 8
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 8; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 9
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 9; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 10
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 10; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 11
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 11; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 12
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 12; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 13
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 13; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 14
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 14; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 15
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 15; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 16
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 16; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 17
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 17; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 18
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 18; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 19
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 19; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 20
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 20; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 21
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 21; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 22
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 22; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_lun:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 23
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 23; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_mar:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mar
              h0_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 24
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 24; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 25
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 25; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 26
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 26; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 27
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 27; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 28
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 28; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 29
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 29; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 30
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 30; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 31
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 31; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 32
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 32; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 33
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 33; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 34
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 34; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 35
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 35; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 36
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 36; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 37
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 37; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 38
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 38; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 39
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 39; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 40
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 40; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 41
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 41; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 42
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 42; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 43
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 43; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 44
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 44; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 45
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 45; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 46
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 46; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_mar:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 47
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 47; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_mer:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Mer
              h0_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 48
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 48; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 49
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 49; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 50
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 50; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 51
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 51; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 52
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 52; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 53
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 53; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 54
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 54; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 55
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 55; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 56
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 56; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 57
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 57; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 58
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 58; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 59
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 59; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 60
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 60; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 61
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 61; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 62
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 62; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 63
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 63; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 64
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 64; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 65
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 65; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 66
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 66; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 67
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 67; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 68
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 68; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 69
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 69; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 70
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 70; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_mer:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 71
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 71; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_jeu:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Jeu
              h0_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 72
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 72; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 73
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 73; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 74
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 74; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 75
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 75; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 76
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 76; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 77
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 77; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 78
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 78; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 79
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 79; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 80
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 80; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 81
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 81; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 82
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 82; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 83
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 83; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 84
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 84; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 85
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 85; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 86
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 86; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 87
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 87; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 88
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 88; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 89
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 89; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 90
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 90; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 91
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 91; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 92
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 92; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 93
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 93; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 94
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 94; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_jeu:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 95
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 95; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_ven:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Ven
              h0_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 96
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 96; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 97
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 97; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 98
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 98; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 99
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 99; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 100
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 100; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 101
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 101; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 102
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 102; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 103
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 103; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 104
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 104; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 105
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 105; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 106
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 106; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 107
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 107; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 108
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 108; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 109
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 109; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 110
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 110; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 111
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 111; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 112
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 112; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 113
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 113; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 114
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 114; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 115
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 115; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 116
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 116; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 117
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 117; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 118
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 118; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_ven:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 119
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 119; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_sam:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Sam
              h0_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 120
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 120; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 121
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 121; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 122
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 122; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 123
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 123; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 124
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 124; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 125
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 125; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 126
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 126; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 127
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 127; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 128
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 128; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 129
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 129; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 130
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 130; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 131
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 131; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 132
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 132; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 133
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 133; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 134
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 134; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 135
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 135; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 136
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 136; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 137
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 137; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 138
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 138; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 139
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 139; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 140
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 140; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 141
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 141; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 142
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 142; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_sam:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 143
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 143; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              label_dim:
                card:
                  type: custom:button-card
                  styles:
                    name:
                      - font-weight: bold
                    card:
                      - border-style: none
                  name: Dim
              h0_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 144
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 144; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h1_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 145
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 145; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h2_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 146
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 146; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h3_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 147
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 147; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h4_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 148
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 148; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h5_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 149
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 149; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h6_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 150
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 150; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h7_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 151
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 151; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h8_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 152
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 152; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h9_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 153
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 153; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h10_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 154
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 154; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h11_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 155
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 155; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h12_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 156
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 156; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h13_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 157
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 157; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h14_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 158
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 158; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h15_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 159
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 159; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h16_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 160
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 160; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h17_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 161
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 161; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h18_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 162
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 162; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h19_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 163
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 163; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h20_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 164
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 164; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h21_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 165
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 165; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h22_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 166
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 166; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
              h23_dim:
                card:
                  type: custom:button-card
                  label: ​
                  show_label: true
                  tap_action:
                    action: perform-action
                    perform_action: script.toggle_prog_bit
                    data:
                      prog: D
                      index: 167
                  styles:
                    card:
                      - border-radius: 5px
                      - min-width: 15px
                      - background-color: |
                          [[[
                            // Récupérer le buffer (string hex)
                            const buffer = states['input_text.progd_buffer'].state;
                            if (!buffer || buffer.length < 56 ) return "red";
                            const index = 167; 
                            // Calcul byte/bit
                            const byte_index = 4 * Math.floor(index / 24) + 3 - Math.floor((index % 24) / 8);
                            const bit_index  = index % 8;
                            const hex = buffer.substr(byte_index * 2, 2);
                            const byte = parseInt(hex, 16);
                            const bit = (byte >> bit_index) & 1;
                            return bit === 1 ? "dodgerblue" : "white";
                          ]]]
          - show_name: true
            show_icon: true
            type: button
            name: Envoyer
            icon: mdi:send-clock
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: ha_vers_esphome
                prog: D
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state: buffer
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-sync
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: D
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state: dataset
            name: Synchroniser
            show_state: false
          - show_name: true
            show_icon: true
            type: button
            name: Tout remplir
            icon: mdi:calendar-month
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progd_buffer
              data:
                value: 00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff00ffffff
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            name: Tout effacer
            icon: mdi:calendar-blank
            tap_action:
              action: perform-action
              perform_action: input_text.set_value
              target:
                entity_id: input_text.progd_buffer
              data:
                value: "00000000000000000000000000000000000000000000000000000000"
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state_not: maj
          - show_name: true
            show_icon: true
            type: button
            icon: mdi:calendar-remove
            tap_action:
              action: perform-action
              perform_action: script.envoie_prog
              data:
                direction: esphome_vers_ha
                prog: D
              target: {}
            grid_options:
              columns: 3
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state: buffer
            name: Annuler
          - show_name: true
            show_icon: true
            type: button
            name: Mise à jour...
            icon: mdi:timer-sand
            grid_options:
              columns: 6
              rows: 2
            visibility:
              - condition: state
                entity: sensor.progd_synchro
                state: maj
            icon_height: 50px
        column_span: 3
        visibility:
          - condition: state
            entity: input_select.programmation_horaire
            state: Rafraîchissement Progr. D
    show_icon_and_title: false
    cards: []
  - type: sections
    max_columns: 4
    title: Consommations
    path: consommations
    icon: mdi:currency-eur
    sections:
      - type: grid
        cards:
          - type: custom:bubble-card
            card_type: separator
            name: Puissance instantanée
            icon: mdi:eye-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: custom:bubble-card
            card_type: button
            button_type: state
            animated_icons: true
            entity: sensor.b_pac_power
            styles: |-
              :host {
                  --color-ok: #bf8d1a;
                  --color-background: var(--bubble-button-main-background-color, var(--bubble-main-background-color, var(--background-color-2, var(--secondary-background-color))));
                  --color-notification-bubble: --color-notification-bubble: #FFFACD;
                  --color-bar: var(--color-ok);
                  /* Calcul de la progress % prod max de 1500 W) */
                  --progress: ${Math.round((Number(state) < 1500 ? Number(state) : 1500) / 1500 * 100)}%;
                }
              .bubble-button-card-container {
                background: var(--color-background) !important;
                position: relative;
              }

              /* Barre de progression qui évolue en fonction de la prod */
              .bubble-button-card-container::before {
                content: "";
                position: absolute;
                left: 0;
                top: 0;
                height: 100%;
                width: var(--progress) !important;
                background: var(--color-bar) !important;
                z-index: 0;
              }
              .bubble-icon {
                color: #bf8d1a !important;
              }
            name: PAC + App ECS Chauf Clim
            icon: mdi:air-conditioner
            sub_button:
              main:
                - entity: sensor.b_pac_power
                  show_state: true
                  show_icon: false
                  state_background: false
                  show_name: true
              bottom: []
            card_layout: large
            grid_options:
              columns: 12
            show_state: false
            scrolling_effect: false
          - type: custom:bubble-card
            card_type: separator
            name: Consommations ECS
            icon: mdi:eye-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS PAC / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.toug_aquaair_conso_eau_chaude
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS appoint / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_ecs_appoint_kwh_annuel
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût ECS / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.ecs_cout_annee
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS PAC / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_ecs_kwh_mois
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS appoint / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_ecs_appoint_kwh_mois
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût ECS / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.cout_ecs_mois
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS PAC / j
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_ecs_kwh_jour
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: ECS appoint / j
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_ecs_appoint_kwh_jour
            grid_options:
              columns: 12
              rows: 1
          - type: custom:bubble-card
            card_type: separator
            name: Consommations Chauffage Clim
            icon: mdi:eye-outline
            grid_options:
              columns: 12
              rows: 0.7
            styles: ""
            card_layout: large-2-rows
            sub_button:
              main: []
              bottom: []
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Chauf PAC / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_chauffage_kwh_annuel
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Chauf appoint / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_chauffage_appoint_kwh_annuel
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût Chauf / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.cout_chauffage_annuel
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Chauf PAC / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_chauffage_kwh_mois
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Chauf appoint / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                      border-radius: 0 0 0 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_chauffage_appoint_kwh_mois
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût Chauf / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.cout_chauffage_mois
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Climatisation / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_climatisation_kwh_annuel
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût Climatisation / an
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.cout_climatisation_annuel
            grid_options:
              columns: 12
              rows: 1
          - type: horizontal-stack
            cards:
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Climatisation / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 32px 0 0 32px !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.salon_toug_aquaair_climatisation_kwh_mois
              - type: custom:bubble-card
                show_state: true
                show_attribute: false
                show_last_changed: false
                force_icon: false
                sub_button:
                  main: []
                  bottom: []
                show_icon: false
                scrolling_effect: false
                card_layout: large
                icon: ""
                name: Coût Climatisation / mois
                button_type: state
                styles: |-
                  .bubble-button-card-container {
                    border-radius: 0 32px 32px 0 !important;
                  }
                attribute: friendly_name
                show_name: true
                card_type: button
                entity: sensor.cout_climatisation_mois
            grid_options:
              columns: 12
              rows: 1
